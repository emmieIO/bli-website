// import { animate, createTimeline, stagger, onScroll } from 'animejs';

// const headline = document.querySelector('.hero-headline');
// const text = headline.textContent.trim();

// // Split into spans
// headline.innerHTML = text.split(' ').map(word =>
//     `<span class="word">${word.split('')
//         .map(char => `<span class="letter">${char}</span>`)
//         .join('')}</span>`
// ).join(' ');

// // Animate each letter
// // const tl1 = createTimeline();
// animate('.hero-headline .word .letter ', {
//     y: stagger(['-2.75rem', '2.75rem']),
//     rotate: { from: stagger('-.125turn') },
//     loop: true,
//     alternate: true
// });

